import { useState } from "react";
import { Button } from "@/components/ui/button";
export default function Newsletter() { const [email, setEmail] = useState(""); const [submitted, setSubmitted] = useState(false); const submit = (event) => { event.preventDefault(); setSubmitted(true); setEmail(""); }; return <section className="bg-[#F5F3EF]"><div className="container max-w-xl py-20 text-center"><h2 className="mb-3 text-3xl font-semibold md:text-4xl">Join The VÉRA List</h2><p className="mb-8 text-muted-foreground">Be first to know about new drops, exclusive offers, and style edits — straight to your inbox.</p>{submitted ? <p className="text-sm font-medium">You&apos;re on the list. Watch your inbox.</p> : <form onSubmit={submit} className="flex flex-col gap-3 sm:flex-row sm:gap-0"><input type="email" required value={email} onChange={(event) => setEmail(event.target.value)} placeholder="Enter your email" className="h-12 flex-1 border border-foreground bg-white px-4 text-sm outline-none focus-visible:ring-2 focus-visible:ring-foreground sm:border-r-0" /><Button type="submit" size="lg" className="rounded-none">Subscribe</Button></form>}</div></section>; }
