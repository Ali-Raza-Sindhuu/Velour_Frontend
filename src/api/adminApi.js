// NOTE: this file already exists in your project (it's the one shown in the
// original AdminDashboard.jsx). This stub exists only so the delivered UI
// compiles standalone before you wire it into your real repo — do NOT
// overwrite your real implementation with this.
//
// TODO(backend): keep your existing implementation of fetchAdminData(), which
// should call your authenticated admin endpoint(s) and resolve to:
// { orders: [], products: [], customers: [], payments: [], promotions: [] }
export async function fetchAdminData() {
  throw new Error("fetchAdminData is a stub — connect it to your real admin API.");
}
